# Base URL of the backend API.
# Expo reads this via app.json -> expo.extra.apiUrl (see src/api/client.ts).
# If you need to override at runtime without editing app.json, you can also
# set EXPO_PUBLIC_API_URL and it will take precedence.
EXPO_PUBLIC_API_URL=http://localhost:4000
